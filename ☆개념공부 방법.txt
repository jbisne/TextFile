객체지향에 대해서 설명해보세요 = 필수 질문

프로젝트에 이런식으로 이렇게 적용햇습니다 하고 [프로젝트]쪽으로 유도를 해야함.

자기소개에도 마지막에 이런 프로젝트를하였습니다.하고 끝

객체지향 4가지 요소 - 프로젝트에 이런게 필요할거같아서 써봤습니다.

자바 -> PDF -> 클래스정리

스프링이란? PDF
=>2.4 스프링 삼각형에데애서 POJO, DI 많이 물어봄 특히 DI (AOP는 잘 안물어봄)

컨테이너 DI 이부분이 제일 중요

혹시 디자인 물어보면 그래픽디자인이아니고 UI UX 쪽으로 맡았다.

☆쓰리티어 구성에 대해서 이해 
-> 클라이언트에서 데이터베이스로 직접적으로 접근을 하지못하니 
서버를 통해서 접근을 하는것
(잘모르겟으면 선생님한테 한번 더 내가 아는게 맞나 확인할것)

FCM 파이어베이스 클라우드 메시지

쓰리티어 - 톰캣이니까 jsp사용해서 JSON으로 만들어서 Web, app으로 내려준것.
보안쪽으로도 좋은게 데이터베이스에 직접 접속이 아니라 좋다. 정해진(짜여진)프로그램대로만
데이터를 가져오기때문.
JSON -> Rest api (화면이 필요없고 데이터만 있는애들)

1티어-서버+데이터베이스 같이 붙어있는것.
2티어 - 웹브라우저
3티어 - 서버와 DB를 붙어져잇으면 서버가 힘들어하기때문에 DB만 사용되는 서버를 따로 분리해주어서
나뉘어준것. (안드로이드가 DB를썼으면 무조건 3티어. 안드로이드는 DB직접 접속이 안되기때문에
웹서버를 통해서만 해야함.)
안드로이드가 db를 사용하려고 하면 웹서버를 통해서 요청을하면 웹서버에서 요청받은 데이터를
db에서 가져와 josn파일형태로 앱에 전송해줌(내 발표에도 적혀있는것.보통 구조정도만 설명해주면
될거다.)
-> 서버가 하나씩 늘어날때마다 티어가 하나씩 늘어나는것.

스프링부트 배울 때 jsp세팅을 할필요없고 서버포트 하나 사용. 그냥 바로 json바로 사용가능.
톰캣안쓰고 부트만 쓰는게 REST쓰고 셋팅 하나도 해줄필요가없기때문.

springboot는 rest api사용에 최적화 된것.
(화면안그릴거면 그냥 셋팅필요없이 써도됨. 우리가 쓴것처럼 일반적으로 써도 되긴함)

오버라이딩 / 오버로딩 (다형성)